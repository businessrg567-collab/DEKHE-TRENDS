## Packages
framer-motion | Essential for the smooth, high-end animations and page transitions required
react-hook-form | Form state management for the lead generation forms
@hookform/resolvers | Validation resolvers for Zod schemas
clsx | Utility for conditional class names
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
